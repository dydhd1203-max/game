# 검사 (테스트 하네스)

이 게임은 파일 하나(`index.html`)라 눈으로만 봐서는 무엇이 깨졌는지 알 수 없다.
그래서 **진짜 브라우저를 띄워 게임을 실제로 돌려 보고** 결과를 세는 검사를 모아 뒀다.
22차 기준 **963항목**이 있다 — 전부 초록이다.

## 처음 한 번만 — 준비

```bash
cd 검사
npm init -y && npm i three          # 게임이 쓰는 three.js (인터넷 막힌 교실에서도 돌게 로컬로)
# playwright 는 대부분의 환경에 이미 깔려 있다. 없으면:
#   npm i -D playwright && npx playwright install chromium
```

`serve2.mjs` 가 이 폴더의 `node_modules/three` 를 읽어서 `index.html` 에 끼워 넣는다
(원본은 unpkg 에서 받아 오는데, 검사 중에는 인터넷을 안 타는 편이 빠르고 안정적이다).

## 돌리는 법

```bash
node t16.mjs ../../클로드/index.html 9000   # <검사파일> <게임파일> <포트>

게임 파일을 안 주면 알아서 찾는다(`gamefile.mjs`). 폴더를 옮겨도 안 깨진다 —
예전엔 검사마다 절대 경로가 박혀 있어서, `클로드/` 아래로 옮긴 뒤 t3~t12 가 통째로 안 돌았다.
```

포트는 검사마다 다르게 준다 (동시에 돌릴 때 안 부딪히게).

전부 한 번에:

```bash
for t in t3 t4 t6 t7 t8 t9 t10 t11 t12 t13 t14 t15 t16 t17 t18 t19 t20 t21 t22 t23 t24 t25 t26; do
  printf "%-4s " $t
  node $t.mjs ../../클로드/index.html $((9000+RANDOM%500)) 2>&1 \
    | awk '/^FAIL/{f++} /  OK  /{o++} END{printf "%d항목 · 실패 %d\n", o+f, f+0}'
done
```

> **`tail` 로 마지막 줄만 보지 않는다.** 예전 반복문은 `tail -2 | head -1` 이었는데,
> 검사마다 마지막 줄이 다르다 — 어떤 것은 합계를, 어떤 것은 `(오류 없음)` 을 끝에 찍고,
> `t3`·`t4`·`t6`·`t7` 은 합계를 아예 안 찍는다. 22차에 `t15` 가 **4개 실패**인 채로
> 전체 판에서는 `(오류 없음)` 으로 보여서 그냥 지나갈 뻔했다.
> **OK/FAIL 줄을 직접 세면** 검사가 어떤 모양으로 끝나든 안 놓친다.

## 무엇을 보는 검사인가

| 파일 | 항목 | 보는 것 |
|---|---|---|
| `t3` | 13 | 지형·통로 — 다섯 입구가 다 열려 있고 수정까지 길이 이어지나 |
| `t4` | 25 | 양·늑대 모형, 인스턴스 메시 개수 |
| `t6` | 34 | 건물·병정·충돌 |
| `t7` | 26 | 아바타 꾸미기(모자·안경·옷) |
| `t8` | 44 | 상인·무기·방어구·물약 |
| `t9` | 28 | 시작 화면·결과 화면·우리 반 기록 |
| `t10` | 67 | 밸런스 상수·사격·데미지 |
| `t11` | 43 | 총 여섯 자루 모형·예광탄·가방(I) |
| `t12` | 31 | 늑대 체력바·할 일 안내·지도 |
| `t13` | 51 | **화면 배치** — 10가지 크기에서 HUD 가 안 겹치나 · 넷리파이 배지 자리 · 최적화 못 박기 |
| `t14` | 43 | 경험치·레벨·스텟(C) · 늑대 강화 · 목표 갈아타기 |
| `t15` | 27 | 이름표 레벨 · 혼자 일어나기 · 연속 명중 · 오늘의 임무 |
| `t16` | 89 | 레벨 25 · 자원 산출 · 공사 시간 · **대장간(무기 강화)** · 남의 무기 보이기 · 레벨 순위 · 벽 체력 · U 수치표 · 18일차 확장 |
| `t17` | 36 | **벽을 뛰어넘는 늑대** — 보스 판정이 표를 보나 · 밤마다 10% 인가 · 실제로 밤을 돌려서 벽을 넘나(통과 아님) · 벽은 안 물고 탑은 무나 · 포물선·자세 · 통신 칸이 안 늘었나 |
| `t18` | 20 | **격자 오각형 맵** — 다섯 입구가 다 가로/세로인가 · 어귀가 직선인가 · 다섯 모둠 다 벽을 한 줄로 놓나 · 구역이 안 뭉쳤나 · **초반 난이도**(기준일이 안 움직이나) · 자원/주머니 · **늑대 분산**(벽 한 칸 정원 · 피해가 퍼지나 · 안 포개지나) · 레벨 35 |
| `t19` | 12 | **오늘의 으뜸**(갈래마다 1등이 갈리나 · 안 한 갈래를 건너뛰나 · 실제로 도나) · **수리 연출**(초록 +숫자 · '살렸다' 일 때만 소식 · pc 통로에 실리나) · 처음부터 다시 하면 여섯 칸이 다 지워지나 |
| `t20` | 58 | **모둠 농장**(다섯이 수정에서 똑같이 떨어졌나 · 먹이·청소 자리가 갈리나 · 비싼 게 더 주나 · 본전이 여러 날 걸리나 · 굶으면 떠나나 · 더러우면 절반 · 자국·파리 · **어느 동물로 꽉 채워도 그릴 칸이 안 넘치나**) · **농장 산물**(아침이면 우리 안에 진짜로 놓이나 · 울타리 안인가 · 서로 안 겹치나 · 기르는 동물에 맞는 것이 나오나 · 몸으로 부딪히면 그 자원이 1 오르나 · 줍기 경험치 · 자원 창에 쌓이나 · 상인 교환 · 자원 여섯 가지에 아이콘·이름이 다 있나 · 산물 줄은 동물이 있어야 뜨나 · 남의 우리 것은 못 줍나) · **상인 바꾸기**(어느 짝·어느 크기로도 손해인가 · 0개를 주지는 않나 · 왕복하면 값어치가 절반 밑인가) · **전직**(레벨 조건 · 고른 길의 칸만 늘어나나 · 길을 못 바꾸나 · 2차 · 늘어난 칸까지 실제로 찍히나 · 차림새가 다 있나 · 제일 많이 걸친 아이도 칸이 안 넘치나) |
| `t21` | 35 | **농장 단장·좌표계**(번호판 글자가 `DIGIT` 표와 같은가 · 판 앞면에 붙었는가 · 대문이 수정 쪽인가) · **통과 안 됨**(울타리·헛간·좌판·화덕 · 실제로 걸어서 막히나 · 뛰어넘어지나 · 거래 거리 안에는 서지나) · **우리가 커지나**(단계·반너비·마당·일하는 자리) · **상점에서 동물 사기**(까닭이 뜨나) · **상인이 살아 있나**(값이 바뀌나 · 고개를 돌리나 · 칸이 안 넘치나) · 우리 앞 `H` 안내 · **자원 자리**(다섯 모둠이 똑같이 받나 · 우리 안에 안 놓이나 · 수정 쪽에 안 몰리나) |
| `t22` | 78 | **하늘 섬 미니게임**(5·10·15일차 밤 뒤에 저절로 열리나 · 섬 높이에 서나 · 섬 밖으로 못 나가나 · O 우리 문·울타리) · **OX 퀴즈**(선생님이 낼 때까지 기다리나 · 단추로 문제가 나가나 · O 안에 서면 잡히나 · 맞히면 자원·틀리면 빈손 · 세 문제 뒤 아침) · **줄넘기**(속도가 오르내리며 늘 앞으로 · 타이밍 맞으면 넘고 아니면 걸림 · 발판 밖은 안 셈 · 줄 다섯이 그려지나 · 모둠 평균으로 순위 · 상) · **서바이벌**(모두 연습용 총 · 같은 모둠·죽은 아이는 안 맞음 · 한 방에 조금씩 · 몸통 열 방 한 목숨 · 목숨 다 쓰면 구경 · 목숨 합계 순위 · 가게에 안 나옴) · **머리 판정**(머리는 몸통의 두 배 · 머리 점을 겨눠야 머리 · 머리 창이 조준 창의 17%) · **우르르 맞기**(셋이 동시에 쏘면 세 번 다 깎이나 · 같은 것이 두 번 오면 한 번만 · 뭉쳐 온 두 발이 두 발만큼) · **서바이벌 지형**(두 단 대·가림벽·상자 · 발이 그 높이를 밟나 · 벽 뒤는 총알이 막히나 · 고리 스폰 · 한 섬 두 얼굴 · 한 사람 몫 넓이 · 덮인 비율 10~35%) · **줄 곡선**(이웃 마디 꺾임 · 마디가 눕는 방향 · 겹침) · **퀴즈 막판**(우리 밖은 오답이고 세어 보여 주나 · 남은 5초 비상 화면) · **소리와 판정 연출**(무기가 부르는 소리가 전부 소리 표에 있나 — 없는 이름은 조용히 아무것도 안 한다 · 미니게임 소리 여섯 · 넘을수록 높아지는 딩 · 정답은 초록 판정 · 오답은 빨간 판정 · 자주 뜨는 것은 작은 판정 · 초록 번쩍) · **OX 문제 현수막**(문제가 나올 때 터지는 소리가 있나 · 번호 딱지와 문제 글이 크게 뜨나 · **큰 글씨로 정한 것이 실제로 큰가** — `font:… inherit` 은 CSS 가 통째로 버려서 16px 이 된다 · 정답이 공개되면 초록으로 바뀌나 · 끝나면 내려가나) · **건물값 20%** |
| `t23` | 84 | **🌷 꽃밭**(마당 안 평평한 빈 칸에만 심어지나 · 수정·산·농장·상인·자원 자리는 막히나 · 꽃끼리 붙으면 막히나 · 모둠당 상한 · 심으면 나무를 쓰고 이름이 남나 · **받은 경험치가 화면에 초록 +숫자로 뜨나** · **낮에만 자라나** · **활짝 핀 꽃 곁에서 체력이 차나** · 물린 직후에는 잠기나(게임의 `sheepHurt` 를 그대로 돌려서) · 새싹·먼 데·미니게임·쓰러짐에는 안 차나 · 여러 송이가 겹쳐도 안 빨라지나 · **다 피어도 그릴 칸이 안 넘치고 여유가 10% 이상인가** · 꽃 키가 0.6칸 아래인가 · 빛깔이 다 밝은가 · 다시 하면 지워지나) · **🐰 짝꿍**(안 데리고 다니면 메시가 숨나 · 스물두 명이 제일 큰 짝꿍을 데려와도 칸이 안 넘치나 · 주인 곁을 안 벗어나나 · 나간 아이 자리가 지워지나 · 번호가 pc 통로로 가나) · **목록**(아이콘·이름 길이가 본 배열과 같나 · **새것이 끝에 붙어 예전 번호가 안 밀렸나** · 제일 많이 걸친 아이의 조각이 칸에 들어가나) · **새 꾸미기가 보이는 창 안에 있나** · **시작 화면이 안 잘리나**(열 가지 크기에서 제목 top 과 '시작 단추까지 닿나' 를 잰다) · 소리 두 가지 |
| `t24` | 38 | **🪢 줄넘기 다시**(스페이스를 꾹 눌러도 한 번만 뛰나 · 줄넘기 뜀이 마을 점프보다 짧고 낮나 · 착지하고 잠깐 못 뛰나 · **발이 줄 높이 위에 있어야 넘나**(예전 `airT<0.16` 봐주기가 남아 있으면 빨개진다) · 연속이 쌓이고 걸리면 0 이 되나 · **모둠 연속이 제일 낮은 사람 숫자인가** · 줄이 갈수록 빨라지나 · **금색 신호가 '눌러도 되는 구간' 안에서 켜지나**(계산이 아니라 실제 뜀 곡선으로) · **손버릇별로 1분을 굴려서** 꾹=0 · 마구<절반 · 금색 보고=거의 다 · 소리 셋) · **3인칭**(줄넘기·퀴즈는 3인칭인가 · 서바이벌·마을은 1인칭인가 · 쓰러짐은 그대로인가 · 3인칭이면 손에 든 것이 숨나 · **뛰어도 카메라가 안 따라 뜨나**) · **내보내기 열쇠를 두 번 쓴 곳이 없나**(겹쳐 쓰면 모양이 바뀌어 옛 검사가 조용히 죽는다) |
| `t25` | 35 | **🎯 서바이벌 다시 (목숨 하나)** — '한 번 쓰러지면 끝' 이 새는 길을 하나씩 막아 본다: 쓰러진 뒤에 또 맞기 · **혼자 일어나기**(마을 시계가 미니게임에서도 돌면 24초 뒤 되살아난다) · 되살아나기 · 마을에 누운 채로 나가기 · 쓰러진 아이가 움직이나 · 총을 내리나 · **시체로 눕나**(PL.down) · **전광판**(사람별 피해 순인가 · 넷까지만 뜨나 · 0 인 아이는 안 뜨나 · 쓰러진 사람에 💀 가 붙나 · 아무도 못 맞혔을 때 안내) · **이긴 모둠 하나**(마지막 생존자의 모둠인가 · 여럿 남으면 피해 제일 많은 사람의 모둠인가 · 전멸이면 · **이긴 모둠은 1등 상을 받나** · **진 모둠도 참가상을 받나** · **참가상이 1등의 1/5~1/2 사이인가**(커지면 '이겨도 그만' 이 된다) · **순위 상 꼴찌보다 많지 않은가**) · **전광판에 못 든 아이도 끝나는 알림에서 자기 피해를 보나**(넷까지라 스물한 명 중 열일곱 명이 못 봤다) · 한 사람/한 모둠만 남으면 일찍 끝나나 · **목숨 상수와 되살아나기 함수가 사라졌나** |
| `t26` | 46 | **🎖️ 칭호** — 남과 겨루지 않는 칭찬. **표**(빠진 칸 · 한 갈래 안에서 문턱과 단계가 오르나 · 표 전체가 쉬운 것→어려운 것인가 · **그림이 다 다른가**(머리 위에 그림 하나만 뜬다) · 단계마다 갈래가 셋 이상인가 · **순위판이 세는 갈래마다 칭호가 있나**(개수를 안 박고 관계로 본다) · **칭호가 읽는 칸이 전부 pc 통로에 이미 오나 = 통신이 안 늘었나**) · **계산**(문턱 경계 · 머리 위는 표에서 제일 뒤 · 친구 것도 같은 함수 · **다음 칭호는 표 순서가 아니라 제일 가까운 것**) · **받는 순간**(게임 숫자에서 잡아내나 · 소식줄에 이름과 조건 · 한가운데 큰 그림 · 두 번 안 뜨나 · **여러 개가 걸려도 하나씩 나오나**(한꺼번에 쏟으면 큰 그림이 서로를 덮어쓴다) · 줄이 하나도 안 사라지나) · **"조금만 더"**(축하 중에는 안 미나 · 한 번만 미나) · 소리가 소리 표에 있나 · **이름표**(그대로면 다시 안 그리나 · 칭호가 바뀌면 그리나 · **어떤 이름·칭호로도 칸 238px 를 안 넘치나** · 친구 머리 위에 실제로 붙나) · **칭호 칸**(스물일곱 칸 · 받은 것만 켜지나 · 다음 칭호와 남은 양 · 못 받은 칸에도 조건이 있나 · 다 모으면 다른 말) · **다시 하기**(칭호와 기억과 줄이 같이 지워지나 · 다시 받아지나) · **친구 소식**(3단계만 뜨나 · 처음 본 친구의 지난 칭호는 안 뜨나) · **우리 반 기록**(모은 칭호 수 · 제일 많이 모은 아이) |

## 재는 것들 (합격/불합격이 아니라 숫자를 뽑는 것)

| 파일 | 무엇을 |
|---|---|
| `bal5.mjs` | 밤을 통째로 돌려 몇 판 버티나 — `node bal5.mjs ../index.html 9000 8 3 3.1 12,15 8 0.75 many 1.36 6` |
| `lvsim.mjs` | 성실한 아이가 며칠에 만렙이 되나 — `node lvsim.mjs ../index.html 9000 0.7` |
| `bdg.mjs` | **칭호 문턱을 정하려고 재는 것** — 부지런함(1 / 0.7 / 0.45)별로 한 사람이 하루에 얼마나 쌓나. 게임 상수로 계산한다(lvsim 과 같은 모형: 캐기는 덩어리 수가, 쏘기는 총알이 한계다). 문턱 후보를 주면 며칠에 닿는지까지 찍는다 — `CAND='{"mined":[40,400,1200]}' node bdg.mjs ../../클로드/index.html 12901` |
| `kite2.mjs` | 도망치는 양을 늑대가 따라잡나 |
| `swap.mjs` | 늑대가 양·건물·수정 사이에서 목표를 갈아타나 |
| `opt2.mjs` | 구간별 CPU 시간 · 쓰레기 수거 압박 · 드로우콜 |
| `opt3.mjs` | 드로우콜이 어디서 나오는지 이름별로 |
| `perf5/perf7` | 시점별·밤중 드로우콜 |
| `boot.mjs` | 여는 데 걸리는 시간 · 메모리 |
| `hud2.mjs` | HUD 가 값이 그대로일 때 DOM 을 안 건드리나 |
| `soak7.mjs` | 몇 밤을 실제로 돌려 오류가 없나 |
| `smoke.mjs` | 그냥 열리나 |
| `err1.mjs` | 콘솔 오류만 빠르게 |
| `lay2.mjs` | HUD 조각들의 실제 좌표를 찍어 본다 (겹침 디버깅용) |

## 도구

- `chk.sh <파일>` — `index.html` 안의 모듈 스크립트만 뽑아 문법 검사 (`node --check`). **고칠 때마다 이걸 먼저 돌린다.**
- `gamefile.mjs` — 게임 파일이 어디 있는지 찾아 준다. 검사에 경로를 박지 않기 위한 것이다.
- `serve.mjs` / `serve2.mjs` — 검사용 서버. `serve2` 가 알맹이고 `serve` 는 옛 이름이다.
  three 빌드 폴더를 통째로 내준다 — r160 이후의 three 는 `three.module.js` 안에서
  `./three.core.js` 를 다시 부르는데, 파일 하나만 내주면 그 요청이 HTML 로 떨어져
  페이지가 통째로 안 열린다.
- `shot14.mjs` — 14차에서 넣은 것들을 화면에 찍어 본다 (대장간·강화창·수치표·순위·친구 총·마지막 밤).
- `shot14b.mjs` — 상인·대장장이 모형과 '내가 든 총' 의 강화 빛을 찍어 본다.
  **HUD 를 잠깐 숨기는 스위치가 들어 있다** — 모형을 볼 때 가운데 안내판이 얼굴을 가려서
  세 번 헛수고했다. 모형을 평가할 때는 이걸 쓴다.
- `gm.mjs` — 손에 든 총 여섯 자루의 실제 크기를 잰다 (빛 조각 크기를 정할 때 썼다).
- `shot35.mjs` — 22차 칭호를 찍고 **자로 잰다** (친구 다섯 머리 위 칭호 · 받는 순간 · 스텟 창 칭호 칸 ·
  이름표가 몇 px 로 줄어드나). **한가운데 큰 그림(verdict)은 1.5초면 꺼진다** — 검사기에서
  evaluate 한 번 왕복이 그보다 오래 걸려서 '그림이 없는' 화면을 세 판 찍었다.
  게임이 만든 markup 은 그대로 두고 **보이기(class)만 되살려** 놓고 찍는다
  (innerHTML 을 다시 넣으면 튀어나오는 애니메이션이 처음으로 되감겨 또 안 보인다).
- `shot34.mjs` — 21차b 참가상 알림을 찍고 **자로 잰다** (몇 줄로 접히나 · 아래 도구 칸에 닿나).
  1100×760 과 교실 크롬북 1366×768 둘 다 본다.
- `shot33.mjs` — 21차 서바이벌을 찍어 본다 (전광판 · 쓰러진 시체 · 이긴 모둠).
  **체력을 손으로 0 으로 만들어 봐야 안 죽는다** — 죽는 길은 `survHit` 하나뿐이다.
  그걸 모르고 '체력 0 인데 멀쩡히 서 있는' 화면을 한 번 찍었다.
- `shot32.mjs` — 20차 줄넘기를 찍어 본다 (줄이 금색이 되는 순간 · 뛰는 순간 · 3인칭 퀴즈).
  **줄이 빨간 것은 '걸렸다' 표시다** — 멈춰 놔도 첫 프레임에 판정이 한 번 돌아서 빨개진다.
  그걸 평소 줄로 착각해서 한참 헤맸다. 찍기 직전에 `__ropeHit(0)` 으로 지운다.
- `shot31.mjs` — 19차에 넣은 것들을 화면에 찍어 본다 (꽃밭·새싹·짝꿍 여섯·새 꾸미기·**내 짝꿍이 1인칭에 보이나**).
  **카메라 방향에 한 번 데었다** — 시선은 `(-sin yaw, -cos yaw)` 다. 부호를 틀리면 엉뚱한 데를 찍는다.
  짝꿍은 손으로 옮겨 놔도 다음 프레임에 제 궤도로 끌려가므로, 붙잡는 대신 **내가 짝꿍 쪽을 보게** 한다.
- `shake.mjs` — 강화 단계별로 손에 든 총이 실제로 얼마나 흔들리는지 잰다.
  정지 화면으로는 떨림을 볼 수 없다 — 여러 프레임의 자리를 모아 폭을 재야 한다.
- `enh.mjs` — 강화 기대 시도 수와 기대 재료를 몬테카를로로 잰다.
- `rep.py` — 정확히 한 군데만 바꾸는 치환기. 여러 군데 걸리면 실패한다 (조용히 엉뚱한 곳을 고치는 사고 방지).

## 검사를 고칠 때의 원칙

**검사가 흔들리면 검사가 아니다.** 이 폴더에서 실제로 겪은 것들:

- 늑대 추격 검사가 *언덕 위*로 달리게 하고 있었다 → 높이차 2.2를 넘으면 늑대는 원래 안 쫓는다. '못 잡았다'가 아니라 '쫓을 수 없었다'였다.
- 같은 검사가 *나무·바위*가 있는 길을 쓰고 있었다 → 늑대가 막힌 것이었다.
- 같은 검사가 *수정 쪽*으로 달리게 하고 있었다 → 늑대는 수정이 코앞이면 양을 놓는 게 맞다.
- 크리티컬 검사가 콤보를 안 끊고 재고 있었다 → 2배가 아니라 2.1배가 나왔다.
- **값을 검사에 박아 뒀다** (14차) — `t4` 가 공사 시간 3.5초·8초를, `t10` 이 금 광맥 산출을,
  `t14` 가 만렙 10과 스텟 상한 5를 숫자로 들고 있었다. 밸런스를 고치자 여덟 항목이 한꺼번에 죽었다.
  게임이 틀린 게 아니라 검사가 값을 복사해 둔 게 틀린 것이다. 이제 `__workSec`·`__STATS` 처럼
  **게임에서 읽어서 관계를 본다** ("벽이 탑보다 튼튼한가", "스텟이 줄어든 값에서 더 줄이는가").
- **떨림이 게임 시간(`tAcc`)을 쓰고 있었다** (14차 손질) — `updHeld` 를 직접 불러 재는
  검사에서는 그 시간이 안 흘러서, 240프레임을 돌려도 늘 같은 값이 나왔다(0 으로 쟀다).
  화면에서는 멀쩡한데 잴 수가 없으면 고쳤는지 확인할 방법이 없다.
  움직이는 것은 **자기 dt 시계**를 갖게 해야 검사에서도 실제와 똑같이 움직인다.
- **강화 확률을 숫자로 박아 뒀다** (15차) — 선생님이 교실에서 겪고 고치는 값이라
  바뀌자마자 죽었다. 값이 아니라 **의미**를 재야 한다:
  "40분 수업 안에 +6 이 나올 만한가(기대 20회 이하)" 가 이 값을 고치는 진짜 이유다.
- **'순간 크기' 로 순서를 쟀다** (14차 손질) — 강화 빛은 숨쉬듯 커졌다 작아지는데,
  그 순간값으로 '단계가 오를수록 커지는가' 를 재니 재는 시점마다 결과가 뒤집혔다.
  흔들리지 않는 둘로 나눴다 — ① 표가 단계마다 커지는가 ② 그려진 크기가 안전한 띠 안인가.
- **경험치를 `XP.xp` 로 쟀다** (14차) — 레벨이 오르는 순간 그 값은 줄어든다.
  레벨 곡선을 눕히자 '강화하면 경험치를 준다' 가 −20 으로 나왔다. 합계로 재야 한다
  (`t15` 는 13차에 같은 이유로 이미 고쳤는데 `t14` 는 안 고쳐져 있었다).

- **진짜 화면 프레임 하나에 기대고 있었다** (21차b) — `t24` 의 줄넘기 검사가 여덟 번에 한 번쯤
  '4연속' 을 '3연속' 으로 내놨다. 게임은 '줄이 몇 바퀴째인가'(`ropeLastTurn`)를 들고 판정하는데
  `goMini` 가 그 칸을 비워(-1) 두고, **비어 있는 채 맞는 첫 바퀴는 기준점이라 판정이 안 난다**
  (맞는 동작이다). 검사는 `enter()` 와 다음 `evaluate` 사이에 실제 프레임이 한 번 돌아
  그 칸을 대신 채워 주는 것에 기대고 있었고, 기계가 바쁘면 그 프레임이 안 돌았다.
  이제 `enter()` 가 한 바퀴를 **직접** 돌려 기준을 잡는다.
  → `node t24.mjs` 를 여섯 개 한꺼번에 돌리면 흔들리는 검사가 바로 드러난다.
- **'제일 가까운 한 쌍' 을 그냥 집었다** (21차b) — `t4` 의 캐기 검사가 서로 가까운 자원 둘을
  찾아 A 뒤에 서서 A→B 를 보는데, 자원 자리는 판마다 새로 나므로 **그 사이에 세 번째가 끼는
  판**이 가끔 나왔다. 그때는 A 대신 그 세 번째가 조준돼 세 항목이 한꺼번에 빨개졌다.
  이제 가까운 쌍을 차례로 놓고 **서 보면서 조건이 실제로 서는 쌍을 찾아** 쓴다.

- **소식줄로 개수를 셌다** (22차) — 칭호가 여러 개 걸렸을 때 '다 나왔나' 를 `#feed div` 수로
  세고 있었다. 그런데 소식줄은 **다섯 줄까지만 남긴다**(`while(f.children.length>5)`).
  여섯 개가 다 나와도 다섯에서 멎으니 영영 안 맞는다. 게임의 줄(`__bgQueue`)과
  받은 것(`__bgGot`)으로 세게 고쳤다. **화면에 남는 개수와 실제 개수는 다른 것이다.**
- **문턱이 작은 갈래로 '조금만 더' 를 쟀다** (22차) — 다음 칭호의 70% 에서 등을 미는지를
  '첫 벽(3채)' 으로 재고 있었다. 3채의 70% 는 2.1채라 올림하면 **곧 문턱**이라,
  밀어 주는 대신 그냥 받아 버린다. 넉넉한 갈래(2단계 캐기 400개)로 잰다.
  **비율을 재는 검사는 반올림이 조건을 삼키지 않을 만큼 큰 값으로 잰다.**
- **'다시 하기' 를 반만 했다** (22차) — 칭호가 지워지는지 보려고 `myReset()` 만 불렀는데,
  레벨은 `MY` 가 아니라 `XP` 가 들고 있어서 레벨 칭호 셋이 살아남았다.
  게임의 단추는 `xpReset()` 도 같이 부른다. **검사가 흉내 내는 동작은 게임이 실제로 하는
  것 전부라야 한다** — 반만 하면 게임이 아니라 검사가 만든 상태를 재게 된다.

무작위(자리·지형)에 기대는 검사는 조건을 **찾아서** 쓴다. 예: `t13` 은 평평한 원을 찾아 쓰고,
`t11` 은 지을 수 있는 칸을 찾아 선다. `t4` 는 서 보고 정한다 (위 21차b).

## 검사와 검사 사이에도 게임은 돈다 (16차)

`pg.evaluate` 하나는 한 턴에 다 돌지만, **evaluate 와 evaluate 사이에는 페이지의
제 루프가 돈다.** 그래서 `hostSim` 을 직접 돌려서 무언가를 재려면 그 전에

- `G.wolves.length = 0` — 남은 늑대
- `W.__spawnQ().length = 0` — **예약된 늑대**. 이걸 안 비우면 hostSim 을 부르는 순간
  오늘 밤 늑대가 통째로 쏟아진다
- `W.__PL.down = true` — **혼자 하기로 들어오면 내 양이 0모둠 산길 한가운데에 선다.**
  늑대가 재려던 짓 대신 내 양을 물어뜯는다

세 줄을 다 해야 한 마리를 깨끗하게 잴 수 있다. 그리고 재는 동안
`G.wolves.includes(w)` 로 내 늑대가 아직 살아 있는지 확인해야 한다 —
`G.wolves.length` 만 보면 남의 늑대가 있는 걸 못 알아챈다.

## 화면 찍을 때 카메라가 되돌아간다 (16차)

`W.__cam.position.set(...)` 로 카메라를 옮긴 뒤 `waitForTimeout` 을 주면,
그 사이 페이지의 `updPlayer` 가 **카메라를 PL 자리로 되돌린다.**
카메라는 반드시 `PL.x/PL.z/PL.yaw` 와 같이 옮겨야 한다.
그리고 `PL.down` 이면 쓰러진 몸이 화면 코앞에 그려지므로, 찍는 순간만 일으켜 세운다
(낮이면 메인 루프가 `hostSim` 을 안 부르므로 늑대는 그 사이 꼼짝 안 한다).

## 하네스도 게임 값을 베끼면 안 된다 (16차)

`bal5` 가 보스를 `w.k < 3` 으로 가리고 있었다. 늑대 표에 한 줄이 붙자
하네스가 엉뚱한 늑대를 보스로 겨눴다. `W.__isBoss(w.k)` 로 **게임에 물어보게** 바꿨다.

## 옛 기록의 숫자와 오늘 잰 숫자를 비교하지 않는다 (16차)

15차 기록의 "2/5" 와 오늘 잰 값을 비교해서 "밤이 쉬워졌다" 고 판단할 뻔했다.
**같은 자리에서 기준(그 기능을 끈 상태)을 다시 재 보니 기준 자체가 그 값이 아니었다.**
바꾼 것의 효과를 재려면 기준도 같은 판에서 같이 재야 한다.

## 검사가 게임 좌표를 손수 계산하면 안 된다 (17차)

`t3` · `t9` · `bal5` 가 통로 자리를 `d.dx*t - d.dz*pp` 로 **직접 계산**하고 있었다.
17차에 입구가 옆으로 밀리자(`DIRS[g].off`) 이 식이 산 한가운데를 가리켜서,
**게임은 멀쩡한데 검사만 빨개졌다.** 전부 게임의 `__gX/__gZ` 에 물어보게 바꿨다.

좌표 변환·구역 나누기·보스 판정처럼 **게임이 이미 답을 아는 것**은
검사에서 다시 만들지 말고 내보내서 쓴다.

## 하네스는 옛 판도 잴 수 있어야 한다 (17차)

"얼마나 바뀌었나" 를 말하려면 **같은 자로 두 판을 재야 한다.**
17차에 이걸 안 해서 한 번 헛 비교를 했다 — 옛 게임을 *옛 하네스* 로 재 놓고
"달라졌다" 고 할 뻔했다(하네스가 달라서 방어선 채 수부터 155 vs 84 로 달랐다).

`bal5` 에 옛 판 대비를 넣어 뒀다. 새 함수가 없으면 예전 식으로 떨어진다:

```js
const GX = (t,p)=> W.__gX ? W.__gX(g,t,p) : oldXf(g,t,p)[0];
```

비교할 때는 **게임 파일만 바꿔서** 돌린다:

```bash
git show origin/main:클로드/index.html > /tmp/old.html
node bal5.mjs /tmp/old.html      12120 10 1 2.0 3,5,7,10 1 0.50 early 1.1 1
node bal5.mjs ../../클로드/index.html 12121 10 1 2.0 3,5,7,10 1 0.50 early 1.1 1
```

## 검사 스스로 매달리게 만들지 않기 (17차)

```js
while(W.__XP.pts>0) W.__takeStat(1);     // ✗ 상한에 닿으면 영영 안 끝난다
for(let i=0; i<stat.max && W.__XP.pts>0; i++) W.__takeStat(1);   // ✓
```

상한이 있는 것을 "다 쓸 때까지" 돌리면 무한 루프가 된다. 10분 넘게 매달렸다.

## 검사와 검사 사이의 상태 (17차)

- `gain()` 은 `base`(모둠 창고)가 아니라 **`G.res`** 에 넣는다. `base` 를 보면 늘 0 이다.
- 앞 검사가 벽을 예순 채 넘게 지어 놓으면 `myPC` 의 '쓴 양' 이 쌓여서,
  창고를 0 으로 두고 재면 `res` 가 0 에 눌린 채 안 올라간다. 넉넉히 채워 놓고 잰다.
- 앞 검사가 `G.me.g` 를 바꿔 놓고 갈 수 있다. 재기 전에 내가 원하는 값으로 두자.

## 기준을 분포 가장자리에 걸지 않는다 (17차b)

`t18` 의 '늑대가 안 포개진다' 를 **0.01% 미만**으로 걸어 뒀는데,
여덟 번 재 보니 관측값이 0~5 라 기준(5.4)이 **분포 가장자리에 딱 걸려**
여덟 판에 한 번씩 빨개졌다. 게임은 멀쩡한데 검사만 흔들린 것이다.

**'얼마나 자주' 대신 '한때 몇 쌍이나' 로 바꿨다.** 밀어내기는 겹친 만큼을 몇 프레임에
걸쳐 푸는 물렁한 장치라, 뒤에서 밀린 늑대 한 쌍이 한순간 깊이 눌리는 건 물리지 버그가 아니다.
눈에 보이는 '무더기' 는 여러 쌍이 **동시에** 포개진 것이다 — 그걸 재야 뜻이 산다.

흔들리는 검사를 만나면 **기준을 느슨하게 하기 전에, 재는 대상이 맞는지부터 본다.**

## G.me.g 와 myPC.g 는 다른 것이다 (17차b)

`t18` 이 열 판에 한 판씩 "캐도 0" 으로 빨개졌다. 원인:

- 자재는 **`myPC.g`** 가 가리키는 모둠 창고로 들어간다 (`recomputeRes`)
- 그런데 검사는 **`G.me.g`** 의 창고를 읽고 있었다
- 이 둘은 페이지의 타이머가 `syncMyPC()` 를 부를 때만 맞춰진다

`G.me.g` 를 바꿨으면 **`W.__syncMyPC()` 를 바로 불러 줘야** 한다.


## 사례 — 검사가 틀렸던 것들 (17차d)

**게임 함수의 부름꼴을 넘겨짚었다.** `farmAt()` 은 인자를 안 받고 '지금 내 양이 선
자리(`PL`)' 를 본다. 검사에서 `farmAt(x, z)` 로 불렀더니 늘 `null` 이 나와서
"일하는 자리가 안 잡힌다" 는 빨간불이 세 개 떴다. 게임은 멀쩡했다.
**새 함수를 검사에 쓰기 전에 그 함수의 첫 줄을 읽는다.**

**`G.res` 에 직접 써 넣었다.** `G.res` 는 `base` + 각자 캔 것 − 각자 쓴 것 으로
**다시 계산되는** 값이다. 여기에 자원을 넣어 봐야 다음 `recomputeRes()` 에서 지워진다.
창고(`__base[g]`, 금은 `.o`)에 넣고 `__recompute()` 를 불러야 한다.
다른 하네스들은 진작 그렇게 하고 있었다 — **새로 짜기 전에 옆 파일을 본다.**

**갈래 개수를 박아 뒀다.** `t19` 가 '갈래가 여섯이다' 를 세고 있어서, 순위판에
농장 갈래를 하나 더하자 게임은 멀쩡한데 검사만 빨개졌다.
개수가 아니라 **관계**('쏘기 말고도 여러 갈래가 남는가')를 보게 고쳤다.

**왕복 손해를 실제 거래로 재려다 엉뚱한 걸 쟀다.** 나무→돌→나무를 실제로 두 번
돌려 봤는데, 두 번째 바꾸기가 방금 받은 돌이 아니라 **쌓아 둔 돌더미에서** 나갔다.
왕복이 아니라 '한 번 바꾸기' 를 재고 있었고 70%라는 그럴듯한 숫자가 나와서
하마터면 통과시킬 뻔했다. 재려는 것이 '흐름' 이면 **재고를 비워 놓고** 재거나,
아예 값어치로 계산해서 본다.

**게임이 새 창을 열게 되자 옛 검사가 빨개졌다.** 17차f에 레벨이 차면 전직 화면이
저절로 열리게 했더니, `t16` 의 '만렙까지 한 번에 올리기' 항목 뒤에 그 창이 떠 있었고
한참 뒤의 'U 로 수치표가 열린다' 가 **이미 열린 창에 막혀** 실패했다.
게임은 설계대로 움직였다. **창을 여는 검사는 제 앞을 스스로 치운다** —
앞 항목에서 치우게 하면, 그 사이에 뭐가 하나 끼는 순간 또 흔들린다.

**앞 항목이 남긴 값 때문에 열 번에 한 번 빨개졌다.** `t20` 의 '아무도 농장을 안 했으면
빈 갈래는 안 돈다' 가, 앞 항목에서 내가 직접 거둔 기록(`MY.farmed`)이 남은 탓에
페이지 타이머가 `syncMyPC` 를 부른 뒤냐 전이냐에 따라 갈렸다.
**'아무도 안 했을 때' 를 재려면 내 기록부터 지운다.**

**시작 화면이 잘리는지는 `t13` 이 열 가지 크기로 본다.** 17차g에 재 보니
1366×768 교실 크롬북에서 카드가 109px 넘쳐 제목이 화면 밖으로 나가 있었다.
겹침만 보지 말고 **넘침(`scrollHeight - clientHeight`)도** 같이 재는 게 좋다.
