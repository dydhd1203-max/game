# 양들의 밤 — 수정을 지켜라

초등학교 3학년 21명이 한 교실에서 같이 하는 브라우저 3D 게임.
다섯 모둠이 각자 산길 입구 하나씩을 맡아, 밤마다 오는 늑대에게서 가운데 수정을 지킨다.

## 어디에 무엇이 있나

저장소 뿌리에서 본 자리다. **게임 파일과 검사 폴더가 서로 다른 갈래에 있다.**

| | |
|---|---|
| `클로드/index.html` | **게임 전부.** 파일 하나에 다 들어 있다 (약 600KB) |
| `클로드/배포용/index.html` | 넷리파이에 올리는 것. 위 파일과 **똑같이 맞춰 둔다**(고칠 때마다 `cp`) |
| `클로드/진행상황.md` | 1차 ~ 23차 작업 기록. **무엇을 왜 그렇게 했는지**가 다 적혀 있다. 새로 이어 붙일 때 **끝부분부터** 읽으면 된다 |
| `깃허브에올릴것/검사/` | 테스트 하네스 **1009항목**. 고칠 때마다 `bash 검사/all.sh` 로 한꺼번에 돌린다 (`검사/README.md` 에 무엇을 보는지 다 적혀 있다) |
| `클로드/개발환경.md` | 교실 기기·배포 환경 |

## 파일 하나 원칙

교실 크롬북은 인터넷이 자주 막히고, 선생님이 파일 하나만 옮기면 되게 해야 한다.
그래서 **빌드 도구도, 번들러도, 프레임워크도 안 쓴다.** three.js 만 CDN 에서 읽는다.

라이브러리를 하나 더 붙이고 싶어지면 그 자리에서 직접 짜는 쪽을 먼저 생각한다
(예: 손에 든 모형을 합치는 코드는 three 의 BufferGeometryUtils 대신 직접 짰다).

## 고치기 전에 알아야 할 것

**움직임은 계산하지 말고 굴려서 잰다** — 20차 줄넘기에서 뛴 높이를 종이 위 포물선으로 잡았다.
`h = v·t − ½g·t²` 로는 최고 0.88칸에 '줄 위에 있는 구간' 이 0.057~0.353초인데,
`updPlayer` 를 실제로 굴려 재 보니 **0.81칸 · 0.05~0.30초**였다(오일러 적분이라 낮게 나온다).
그걸 모르고 신호를 0.34초 전에 켰더니 "신호 보고 눌렀는데 걸린다" 가 31번 중 29번이었다.
**타이밍이 걸린 값은 반드시 게임 함수를 그대로 돌려서 잰다.**

**보이는가를 자로 잰다** — 19차에 짝꿍(따라다니는 작은 동물)을 넣고 "귀엽다" 로 끝낼 뻔했다.
1인칭 눈높이는 1.12칸인데 짝꿍 키는 0.5칸이라, 곁에 붙을수록 화면 **아래로** 내려간다.
처음 반지름(1.25칸)에서는 아래 도구 칸(1366×768에서 top 624px)에 통째로 가려 있었다 —
코드는 맞고, 스크린샷에도 '없으니까' 안 보인다. 반지름별로 찍히는 y 를 계산해 보고 2.6칸으로 잡았다.
**화면 어디에 찍히는지까지가 기능이다.**

**성능** — 교실 크롬북(Mali-G72 MP3)은 **드로우콜 하나가 약 60마이크로초**다.
삼각형보다 그리는 횟수가 훨씬 비싸다. 그래서:
- 같은 것이 여러 개면 인스턴스 메시로 묶는다
- 손에 든 모형처럼 안 움직이는 조각은 재질끼리 미리 합쳐 굽는다
- 뱅크(bpush) 하나는 **첫 번째로 넣은 모양으로 고정**된다 — 한 열쇠에 여러 모양을 섞으면 나머지가 다 첫 모양으로 그려진다

**HUD** — `paintHUD` 는 초당 8번 돈다. `setTxt`/`setSty` 는 **값이 바뀔 때만** DOM 을 건드린다.
이 버릇을 깨면 크롬북에서 바로 티가 난다 (`검사/hud2.mjs` 가 지킨다).

**화면 배치** — 손으로 `top`/`bottom` 숫자를 잡지 않는다. 세로 flex 로 묶거나 JS 로 재서 잡는다.
(그 숫자 때문에 세 번 겹쳤다.) 짧은 화면에서 겹치는지는 `검사/t13.mjs` 가 10가지 크기로 본다.
CSS 미디어 쿼리는 **같은 힘이면 나중에 쓴 것이 이긴다** — 더 좁은 조건을 뒤에 둔다.

**늑대 길찾기** — `walkable()` 은 `terrH`(지형)만 본다. `bldH`(건물)는 안 본다.
그래서 망루 위는 늑대 눈에 안 보인다. 높이차 `SHEEP_REACH_Y = 2.2` 를 넘으면 쫓지도 물지도 않는다.

**통신** — 자리(위치)는 초당 13번 나가는 통로다. 여기에 칸을 더하지 않는다.
레벨처럼 가끔 바뀌는 것은 `pc` 통로(초당 1회 남짓)에 얹는다.

## 고칠 때의 순서

1. `bash 검사/chk.sh <게임파일>` — 문법부터 (한 줄 잘못 넣고 30분 헤맨다)
2. 고친다
3. 관련 검사를 돌린다 — 검사 폴더 안에서 `node t16.mjs ../../클로드/index.html 9000`
   (전부 돌리려면 `bash 검사/all.sh` — 동시에 돌려서 몇 분이면 끝나고 **빨간 것만** 찍는다)
4. **화면에 찍어 본다** — 코드가 맞아 보여도 화면은 다를 수 있다.
   거꾸로 달린 총, 안 보이던 체력바, 얼굴에서 터진 축포를 다 스크린샷이 잡았다
5. `cp 클로드/index.html 클로드/배포용/index.html` 로 맞춘다
6. `클로드/진행상황.md` 끝에 **무엇을 왜** 했는지 적는다 (차수를 하나 올려서)

## 검사가 흔들리면 검사가 아니다

무작위(지형·자리)에 기대는 검사는 조건을 **찾아서** 쓴다.
빨간불이 떴을 때 게임이 아니라 검사가 틀린 경우가 여러 번 있었다 — `검사/README.md` 에 사례가 있다.

**혼자 돌려서 통과한다고 흔들리지 않는 게 아니다.** 21차b에 `t24` 가 전체 판에서 3개 실패였는데
혼자서는 일곱 번 내리 통과했다. 여섯 개를 한꺼번에 돌려 기계를 바쁘게 만들자 세 번에 한 번 재현됐다 —
검사와 검사 사이에 **진짜 화면 프레임이 한 번 돌아 주는 것**에 기대고 있었던 것이다.

```bash
for i in 1 2 3 4 5 6; do ( node t24.mjs ../../클로드/index.html $((9800+i)) ) & done; wait
```

그래서 **전체 검사는 하나씩이 아니라 한꺼번에 돌린다** — `bash 검사/all.sh` 가 동시에 네 개씩
돌린다. 빠른 건 덤이고(20분 → 몇 분), 본래 목적은 **기계를 바쁘게 해 두는 것**이다.
한가한 판이 필요한 것은 성능 측정뿐이고 그건 `all.sh` 와 같이 돌리지 않는다.

**그리고 전체를 한 번 돌려 초록이 나왔다고 끝난 게 아니다.** 같은 21차b에 다음 판에서야
`t4` 가 빨개졌다(자원 자리가 판마다 새로 나서 세팅이 무너지는 판). 손보고 나면 **두 번은 돌린다.**
바쁜 기계에서 두 번이면 한가한 기계에서 네 번보다 많이 잡힌다 — 23차에 직렬 판에서 한 번 나온
`t13` 빨간불을 직렬로 두 판 더 돌려도 못 잡았는데, **같이 돌리자 한 번에 나왔다.**
**판 수를 늘리는 대신 판을 바쁘게 만든다.**

그때 걸린 것이 '색칠이 다시 만들기보다 10배 빠른가' 라는 **시간 비율** 항목이었다.
느려진 쪽은 색칠 한 판뿐이고 다시 만들기는 멀쩡했다 — 게임이 느려진 게 아니라
**기계가 멈칫한 한 번이 평균에 섞인 것**이다. 문턱을 느슨하게 하지 않고
**세 판을 재서 제일 빠른 판을 쓰게** 고쳤다(멈칫은 값을 늘리기만 하므로 최솟값에는 안 섞인다).
**시간을 재는 항목은 평균을 쓰지 않는다** — 전체 판이 늘 바쁜 기계에서 도니까.

## 1인칭이 늘 답은 아니다

**판정하는 것이 화면에 보여야 한다.** 20차 줄넘기는 '발이 줄 높이 위에 있나' 로 판정하는데,
1인칭에서는 자기 발이 안 보인다. 재 보니 판정하는 순간 줄은 시선보다 **62° 아래**라
아래 도구 칸에 통째로 가려 있었다(바닥 그림자를 깔아 봤지만 반지름이 1.35칸뿐이라
그림자도 늘 발치라 똑같이 안 보였다 — 넣었다가 도로 뺐다).

그래서 줄넘기·OX 퀴즈는 **3인칭**이다. 서바이벌은 겨누는 판이라 1인칭 그대로다.
카메라를 빼는 장치는 '쓰러졌을 때'(9차) 것을 그대로 쓴다 — 거리만 다르다.
**3인칭 카메라 높이는 땅에 고정한다.** 몸을 따라 같이 뜨면 뛰어도 줄과의 위아래 관계가
화면에서 그대로라 '넘고 있다' 가 안 보인다.

## 잘하는 방법이 여러 갈래로 보여야 한다

이 게임이 소리 내어 칭찬하는 것이 오랫동안 전부 '쏘기' 였다 —
처치 알림 · 연속 명중 · 강화 무기 빛 · 보스 막대 · 레벨 순위.
**밤새 벽을 고치고 자원을 캐 온 아이는 아무 흔적도 안 남았다.**

순위판은 여덟 갈래(레벨·캐기·짓기·수리·도움·명중·농장·꽃)를 5초씩 돌린다.
**갈래 개수를 검사에 박지 않는다** — 17차d에 농장 갈래를 더하자 게임은 멀쩡한데
검사만 빨개졌다. 세어야 할 것은 개수가 아니라 '길이 여러 갈래로 남는가' 다.
새 장치를 넣을 때는 **"이걸 잘한 아이의 이름이 어디에 남는가"** 를 같이 정한다.

**그리고 겨루는 칭찬만 늘리지 않는다.** 22차에 세어 보니 소리 내어 칭찬하는 장치가
전부 둘 중 하나였다 — 남보다 잘했거나(순위판·으뜸·전광판·등수 상), 3초 뒤에 사라지거나(알림).
**스물한 명 중 여섯째로 잘한 아이는 하루 종일 하고도 아무 데도 안 남는다.**
칭호(`BADGES`)는 그래서 **문턱만 넘으면 누구나** 받는다. 21차b 참가상과 같은 이유다 —
"아무것도 못 받으면 다음엔 안 하려 든다."

칭호가 남는 자리는 **머리 위 이름표**다. 순위판은 5초마다 갈리고 알림은 3초면 사라지는데,
이름표는 그 아이가 화면에 있는 내내 떠 있다. 그리고 **같은 단계 안에서는 드문 것을 표 뒤에 둔다** —
머리 위에는 '표에서 제일 뒤에 있는 내 칭호' 가 뜨므로, 캐기(누구나 한다)를 앞에,
친구 일으키기·옆 모둠 돕기·꽃·농장을 뒤에 두어야 아이마다 그림이 갈린다.
**스물한 명이 다 같은 그림을 달고 있으면 그건 칭호가 아니다.**

## CSS 는 순서가 아니라 힘이 이긴다

23차에 가방을 넓히려고 `.popC.wide{width:min(880px,100%)}` 를 **맨 뒤에** 썼는데
아무 일도 안 일어났다. 이 파일에는 이미 `#popKit .popC{width:min(720px,94vw)}` 가 있고,
**아이디(1,0,0)가 클래스 둘(0,2,0)을 이긴다.** 나중에 썼는데도 졌다.

미디어 쿼리 이야기(같은 힘이면 나중 것이 이긴다)와 헷갈리기 쉽다 — 힘이 다르면 순서는 안 본다.
**폭·자리를 바꿀 때는 새 규칙을 만들지 말고 이미 있는 그 줄을 고친다.**
그리고 바꾼 뒤에는 반드시 **실제 폭을 재 본다** — 안 먹은 규칙은 조용히 아무 일도 안 한다.

## 스탯 상한을 올릴 때는 칸당 값을 같이 내린다

23차에 능력 여섯(캐는 손·큰 주머니…)을 스탯 넷(힘·체력·민첩·지능)으로 바꾸면서
상한을 8 → 12 로 올렸다. 넷뿐이라 8이면 레벨 35(점수 35개)에 4×8=32 — **다 찍고도 점수가 남아**
고르는 게임이 아니게 되기 때문이다.

그런데 칸당 값을 그대로 두었더니 재 보니 세기가 통째로 부풀었다(체력 276→412 · 공격 ×1.96→×2.80).
**그러면 늑대 세기·자원·레벨 곡선을 전부 다시 재야 한다.**
칸당 값을 내려 **12칸이 예전 8칸과 같아지게** 맞추면 지금까지 잰 밸런스가 그대로 산다 —
바뀐 것은 세기가 아니라 고르는 방식이다. `검사/statbal.mjs` 가 이걸 옛 판과 번갈아 잰다.

**겹치되 주·곁이 분명해야 한다.** 캐는 속도는 민첩(0.92/칸)이 크게, 힘(0.98/칸)이 조금 올린다.
겹치면 "내가 찍은 것도 여기에 쓸모가 있네" 가 생기고 잘못 찍어도 버려지지 않는다.
둘이 똑같이 올리면 고를 이유가 사라진다.

## 새 능력이 늑대 추격을 깨뜨리는지 먼저 본다

양은 초속 5.4, **날쌘늑대는 5.35** 다. 종이 한 장 차이다.
23차에 민첩에 이동 속도를 붙일 때 여기를 크게 줬으면 날쌘늑대가 영영 못 쫓게 된다.
그래서 칸당 1.5%(다 찍어도 +18%)로 묶고 `t27` 이 그 상한을 지킨다.

속도·사거리·조준처럼 **쫓고 쫓기는 관계에 닿는 값**은 넣기 전에 표에서 제일 빠른 늑대와
맞대 본다. `kite2.mjs` 로 기준선이 안 움직였는지도 같이 본다.

## 축하는 겹치면 둘 다 안 읽힌다

한가운데 큰 그림(`verdict`)은 **연달아 부르면 뒤엣것이 앞엣것을 덮어쓴다.**
22차 칭호는 첫날에 문턱 두세 개를 동시에 넘는데, 그때 다섯 번을 한꺼번에 불렀더니
**소식줄만 다섯 줄이고 축하는 한 번**이었다. 오류는 안 난다.
여러 개가 한꺼번에 걸릴 수 있는 축하는 **줄을 세워 하나씩** 내보낸다(2초 간격).

그리고 **`#toast` 와 `verdict` 는 같은 자리를 쓴다.** 알림은 화면 한가운데에서 70px 아래인데,
큰 그림의 이름 글자가 바로 거기 앉는다. 둘을 같이 띄우면 겹쳐서 둘 다 안 읽힌다 —
하나만 쓰고, 설명은 왼쪽 소식줄(`feed`)에 적는다. 거기는 안 겹치고 8초를 머문다.

**둘 다 코드로는 멀쩡하다. 화면을 안 찍었으면 교실에서 발견했을 것이다.**

## 잘라 놓은 목록에서 '내 것' 을 찾지 않는다

21차 서바이벌은 끝날 때 "(내가 넣은 피해 45)" 를 띄우는데, 그 값을 **전광판에서 내 이름을 찾아**
가져오고 있었다. 전광판은 `slice(0, 4)` 로 잘라 놓은 목록이다 —
**스물한 명 중 열일곱 명은 그 줄이 통째로 안 떴다.** 오류는 안 난다. 그냥 조용히 빈 글자다.
정작 그 줄이 필요한 아이들이다(전광판에도 못 들고 상도 못 받은 아이).

`MINE.dm` 처럼 **내 값은 내 기계에 늘 있다.** 남에게 보여 주려고 잘라 놓은 목록은
남을 보여 줄 때만 쓴다. 덤으로 이름으로 찾는 것도 위험하다 — 한 반에 같은 이름이 둘이면
남의 숫자가 뜬다(21차b에 둘 다 같이 고쳤다).

## 맵 좌표는 한 곳에서만 만든다

입구마다 방향(`dx,dz`)과 **옆으로 밀린 정도(`off`)** 가 있다.
이걸 여기저기서 손으로 더하고 빼면 반드시 어긋난다. 변환은 네 줄로 못 박혀 있다 —
`gT` · `gPP` · `gX` · `gZ`. **게임도 검사도 하네스도 전부 이걸 거친다.**

같은 이유로 **구역 나누기는 각도로 하지 않는다.** 아래쪽 두 입구는 방향이 똑같고
옆으로만 갈라져 있어서 각도로는 구분이 안 된다 — `sectorOf` 는 '어귀에서 제일 가까운 곳' 을 본다.

## 마을 경계는 네모, 세상 끝은 원

칸이 정사각형이라 **완전한 직선은 가로·세로뿐**이다. 그래서 마당 경계는
모서리만 깎은 네모(`oct`)다 — 다섯 입구 어귀가 전부 직선이라야
아이들이 벽을 한 줄로 긋고 그 끝이 산에 맞물린다.
세상 끝(`FIELD_R`)은 그대로 원이라, 위에서 보면 둥근 산 안에 네모난 마당이다.

## 칸 하나를 없앨 때는 그 칸을 읽던 자리를 전부 찾는다

21차에 서바이벌 목숨(`MINE.l`)을 없앴다. 그런데 조준 코드에
`if(d && !(d.l|0)) continue;` — '목숨을 다 쓴 아이는 안 맞는다' 가 있었다.
`l` 칸이 사라지자 **모두가 그 조건에 걸려서 아무도 조준이 안 됐다.**
오류는 안 난다. 화면에서는 "총이 안 나가요" 로 보인다.

칸을 지우기 전에 `grep` 로 그 이름을 **전부** 훑는다 —
`MINE.l` 뿐 아니라 통신으로 오는 `d.l` 까지.

## 규칙을 바꾸는 것과 '새는 길을 막는 것' 은 다른 일이다

'한 번 쓰러지면 끝' 을 만들려고 목숨을 1로 줄였는데, 되살아나는 길이 **네 개**였다:
되살아나기 함수 · **마을의 혼자 일어나기 시계(`downTick`, 24초)** · 쓰러진 뒤에도 맞기 ·
마을로 누운 채 나가기. 하나만 남아도 규칙이 통째로 무너진다.

새 규칙을 넣을 때는 **"이 규칙을 피해 가는 길이 어디인가" 를 먼저 세어 본다.**
`t25` 는 그 길을 하나씩 다시 걸어 본다.

## 검사용 내보내기 열쇠를 두 번 쓰지 않는다

파일 맨 아래 `Object.assign(window, {...})` 는 400줄이 넘는다. 여기에 이미 있는 이름을
또 쓰면 **나중에 쓴 것이 이긴다.** 값이 같으면 아무 일도 안 일어나지만,
모양이 바뀌면(물건 → 함수) 그 이름을 쓰던 옛 검사가 통째로 죽는다.

20차에 `__held:held` 가 있는데 `__held:()=>held` 를 또 써서 `t11` 이 죽었다.
14차에 `__STATS` 로 같은 일을 겪고 주석까지 달아 뒀는데 또 밟았다.
**오류가 안 난다 — 조용히 딴 것이 나간다.**

`t24` 가 파일 글자를 직접 훑어 중복 열쇠를 센다(브라우저 안에서는 이미 합쳐진 뒤라 못 본다).
지금은 **452개 전부 한 번씩**이다. 하나라도 두 번 쓰면 빨개진다.

## 종류를 번호로 가리지 않는다

늑대 종류(`WOLF_T`)·건물 종류처럼 **표에 줄이 늘어날 수 있는 것**을
`k >= 3` 같은 번호로 가리면, 표에 한 줄 붙이는 순간 열 군데가 조용히 틀어진다.
표의 칸을 보게 한다 — `isBoss(k)` 는 `WOLF_T[k].boss` 를 읽는다.

같은 이유로, **종류 번호로 색인하는 배열은 표와 길이가 같아야 한다.**
`SHEEP_BITE[w.k] || 10` 은 표가 밀렸을 때 오류를 내는 대신
**조용히 기본값으로 떨어진다** — 14차에 늑대왕이 이렇게 제일 약해졌고
16차에야 발견됐다. 검사에 "종류마다 빠짐없이 값이 있나" 를 넣어 둔다.

## 어두운 색을 칠하면 검은 상자가 된다

색은 텍스처에 **곱해진다** — 밝게는 못 하고 어둡게만 된다.
4차에 바위, 8차에 대장간, 17차d에 우리 바닥 자국까지 **세 번** 같은 데 빠졌다.
"흙색이니까 진갈색" 하고 `0x4a3a24` 를 주면 화면에는 새까만 상자가 놓인다.
바탕보다 **조금** 어두운 정도로 잡고, 반드시 찍어 본다.

## 인스턴스 칸은 제일 많이 쓰는 종류로 잡는다

`imesh(geo, mat, N, 1)` 의 N 이 모자라면 **오류가 안 난다 — 조용히 잘린다.**
17차d에 꾸밈 조각을 마리당 3칸으로 잡았는데 닭 한 마리가 넷을 썼다(볏 셋 + 부리).
우리를 닭으로 채웠을 때만 티가 났다.
'평균' 이 아니라 **제일 많이 쓰는 종류로 꽉 채웠을 때**를 기준으로 잡고,
검사에 `count <= instanceMatrix.count` 를 넣어 둔다.

17차f에 또 걸렸다 — 꾸미기 칸이 한 명당 18개인데 모자 5 + 안경 7 + 옷 6 = **18** 이라
여유가 한 칸도 없었고, 전직 차림새를 얹자 그냥 잘렸다. **딱 맞는 칸은 모자란 칸이다.**

19차에 세 번째다. 그때 40으로 올려 둔 칸이 모자 5 + 안경 7 + 옷 8 + 전직 20 = **40** 이었고,
꽃밭 칸도 일흔 송이 × 8조각 = **560/560** 이었다. 둘 다 새것 하나만 더하면 잘릴 자리였다.
이제 검사(`t23`)가 표에서 최대값을 세어 칸과 맞대 보고 **여유가 10% 이상**인지까지 본다 —
개수를 검사에 박지 않으므로 표에 줄이 늘어도 검사가 저절로 따라온다.

## 양 몸에 무엇을 붙일 때는 '보이는 창' 을 먼저 본다

몸통 앞면은 `f≈0.47`, 머리는 `f 0.37~0.99` · `u 0.74~1.38` 을 차지한다.
그래서 **가슴에서 실제로 보이는 창은 `u 0.25~0.74` 뿐이다.**
여기를 벗어나면 몸이나 머리 속에 파묻혀 화면에 아예 안 나온다
(볼터치·전직 하트·가슴판이 차례로 여기서 사라졌다).
새 장식은 이미 있는 `CLOTHES`·`HATS` 가 쓰는 자리를 그대로 따르는 게 제일 빠르다.

## 실루엣이 갈려야 종류가 갈린다

전직 세 벌을 다 '넓은 챙 + 돔' 으로 만들었더니 멀리서 셋이 똑같아 보였다.
색을 다르게 하는 것으로는 안 된다 — 교실 화면은 작고 아이들은 멀리서 본다.
**모양 자체가 달라야 한다** (챙 없는 투구 / 앞으로만 나온 챙 / 넓은 밀짚 챙).

같은 이유로 **모둠 색과 겹치는 색을 장식 바탕에 쓰지 않는다.**
전투양 갑옷을 분홍으로 했더니 1모둠(빨강) 양에서 통째로 묻혔다. 금빛은 다섯 색 어디서도 산다.

## 목록에 한 줄 넣을 때는 '가운데' 가 아니라 '끝' 에 넣는다

모자·안경·옷은 **번호로** 저장되고 **번호로** 친구들에게 전송된다.
17차g에 새 옷을 배열 가운데 끼웠더니, 예전에 저장해 둔 아이들 착장이 통째로 밀렸다.
새것은 끝에 붙인다. 그리고 **아이콘·이름 배열의 길이가 본 배열과 같은지 꼭 확인한다**
(`SHEEP_BITE` 때와 같은 함정이다).

## 화면이 잘리는지는 눈이 아니라 자로 잰다

시작 화면 카드가 **1366×768 교실 크롬북에서 109px 넘쳐** 제목이 잘려 있었다.
눈으로는 "좀 기네" 정도라 몇 판을 그냥 지나쳤다.
`scrollHeight - clientHeight` 를 교실 크기들(1366×768 · 1280×800 · 1024×768 · 태블릿)에서
재 보면 한 줄로 나온다. 고칠 때도 숫자로 `top` 을 잡지 말고 **격자를 나눈다** —
17차g는 넓은 화면에서 고르는 칸을 두 줄로 앉혀 180px 를 줄였다.

**그런데 카드를 줄이는 것만으로는 안 끝난다.** 19차에 재 보니 17차g 이후로도
1024×600·태블릿 세로에서는 제목이 계속 잘려 있었다(top −31 · −130). 진짜 원인은 길이가 아니라
`#title{align-items:center}` 였다 — 가운데 정렬은 내용이 화면보다 길어지면 넘친 만큼을
위아래로 반씩 밀어내고, **위로 밀려난 부분은 스크롤로도 못 간다.**

```css
#title{ align-items:center }        /* 넘치면 위가 잘린다 */
#title{ align-items:safe center }   /* 넘치면 가운데 대신 위에 붙인다 */
```

`safe center` 를 뒤에 한 줄 더 쓰면 된다. 모르는 브라우저는 그 줄만 조용히 버리고
윗줄을 쓰므로 되돌아가도 지금까지와 똑같다. **화면을 채우는 카드에는 이걸 같이 쓴다.**
`t23` 이 열 가지 크기에서 제목 top 과 '시작 단추까지 닿는가' 를 잰다.

## 밸런스는 재서 정한다

느낌으로 숫자를 고치지 않는다. `검사/bal5.mjs` 로 밤을 다섯 판씩 돌려 보고,
`검사/lvsim.mjs` 로 며칠에 만렙이 되는지 계산한다.
`진행상황.md` 에 지금까지 잰 표가 다 있다.

**성능은 아무것도 안 돌리는 상태에서 잰다.** 17차g에 검사를 백그라운드로 돌려 둔 채
CPU 를 쟀더니 1.5~4.9ms 로 널뛰었고, 하마터면 "두 배가 됐다" 고 적을 뻔했다.
조용한 상태에서 예전 판과 **번갈아 세 번씩** 재면 흔들림이 바로 보인다.

**기준도 같은 판에서 같이 잰다.** 옛 기록의 숫자와 오늘 잰 숫자를 비교하면 안 된다 —
16차에 15차 기록의 "2/5" 와 비교해서 "쉬워졌다" 고 판단할 뻔했는데,
같은 자리에서 기능을 끄고 다시 재 보니 기준 자체가 그 값이 아니었다.

## 선생님 설정

`index.html` 맨 위 `window.DEFAULTS` 에 낮/밤 길이, 목표 날짜, 수정 체력, **늑대 세기**가 있다.
반이 너무 쉬워하거나 어려워하면 `wolfPower` 를 만진다 (0.7 쉬움 / 1.0 보통 / 1.4 어려움).
