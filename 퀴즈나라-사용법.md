# 퀴즈나라 — 우리 반 퀴즈 게임

마지막 갱신: 2026-09-02

큐플레이처럼 **퀴즈를 풀어 골드를 모으고 아바타를 꾸미는** 게임이다.
아이들은 크롬북 브라우저로 들어오고, 아이디·비밀번호로 골드와 옷이 저장된다.

파일은 **`퀴즈게임/index.html` 한 개**다. (양들의 밤처럼 두 벌을 맞출 필요 없다)

---

## 1. 처음 한 번만 — 파일 맨 위 고치기

`퀴즈게임/index.html` 을 열면 맨 위에 **★ 선생님 설정 구역 ★** 이 있다. 거기만 고치면 된다.

```js
window.TEACHER_CODE = "1234";        // 선생님 화면 암호 — 꼭 바꿔라
window.CLASS_NAME   = "3학년 2반";   // 화면 위에 뜨는 반 이름
window.FIREBASE_CONFIG = { ... };    // 양들의 밤 것 그대로 써도 된다
```

**파이어베이스는 양들의 밤과 같은 걸 써도 된다.**
이 게임은 데이터를 전부 `quiz` 아래에만 쓰고, 양들의 밤은 `sheepnight` 아래에 쓴다. 안 섞인다.

새 프로젝트를 만들고 싶으면 파이어베이스 콘솔에서
**Realtime Database** 를 만들고(위치는 `asia-southeast1` 이 가깝다) 설정을 통째로 복사해 붙이면 된다.

### 데이터베이스 규칙

파이어베이스 콘솔 → Realtime Database → **규칙** 에 이렇게 넣는다.

```json
{
  "rules": {
    "sheepnight": { ".read": true, ".write": true },
    "quiz":       { ".read": true, ".write": true }
  }
}
```

맨 위에 `".read": true, ".write": true` 하나만 두면 **다른 게임 데이터까지 한 덩어리로 열린다.**
위처럼 게임별로 나눠 두면 나중에 하나만 잠그기 쉽다.

---

## 2. 배포 — 넷리파이

**`퀴즈게임/` 폴더를 통째로** 넷리파이 Deploys 탭에 드래그앤드롭한다.

- 양들의 밤과 **다른 사이트**로 만들어라. (넷리파이에서 Add new site → Deploy manually)
- 고칠 때마다 같은 사이트의 Deploys 탭에 다시 드래그하면 된다.
- 이 폴더에는 `index.html` 과 `_headers` 만 있다. **한쪽만 고치는 실수가 없다.**

아이들한테는 그 사이트 주소만 알려 주면 된다.

---

## 3. 첫 수업 하는 순서

### 선생님

1. 게임 주소 열기 → 오른쪽 아래 **선생님** 버튼 → 암호(`TEACHER_CODE`) 입력
2. **문제 만들기** 탭 → `+ 새 묶음` → 이름 적기 (예: "3학년 1학기 수학")
3. `📋 여러 개 붙여넣기` 에 문제를 통째로 붙여넣고 **넣기**
4. **수업 열기** 탭 → 게임 종류 고르고 → 문제 묶음 고르고 → **방 열기**
5. 아이들이 다 들어오면 **첫 문제 내기**
6. 다 풀면 **점수 매기고 끝내기** (1·2·3등에게 보너스 골드가 나간다)

### 아이들 (처음 한 번)

1. 게임 주소 열기
2. **처음이에요 (가입)** 누르기
3. 아이디 적기 (한글 별명도 된다) → 숫자 4개 비번 → 한 번 더
4. 끝. 다음부터는 같은 아이디·비번으로 들어오면 골드와 옷이 그대로 있다.

**같은 크롬북을 계속 쓰면 다음부터는 저절로 들어가진다.**
크롬북을 바꿔 앉았거나 저절로 안 들어가지면 아이디·비번을 치면 된다.

> 비번을 잊은 아이는 **선생님 화면 → 학생 명단 → 비번 초기화** 를 누르면 `0000` 이 된다.

---

## 4. 게임 세 가지

| 종류 | 어떻게 하나 | 어떤 문제가 필요한가 |
|---|---|---|
| **일반 퀴즈** | 문제가 뜨면 다 같이 답을 낸다. 빨리 맞힐수록 점수가 높다 | 아무 문제나 |
| **OX 게임** | O 쪽이나 X 쪽으로 옮겨 간다. 틀리면 탈락, 끝까지 남으면 보너스 150골드 | 정답이 `O` 또는 `X` 인 문제만 |
| **가로세로** | 낱말 격자를 다 같이 채운다. **먼저 맞힌 사람이 그 칸을 차지**한다 | 정답이 2~7글자인 문제 |

여기에 더해 **금요일마다 패션왕 대회**가 있다 (아래 6-2번).

가로세로는 한글이라 **글자가 통째로 겹쳐야** 이어 붙는다(영어처럼 자모로 못 쪼갠다).
겹칠 데가 없는 낱말은 옆 빈자리에 따로 놓는다. 겹치는 글자가 많은 낱말을 섞어 두면 격자가 예뻐진다.

---

## 5. 문제 쓰는 법

한 줄에 한 문제. 칸은 **탭**이나 **쉼표**로 나눈다.
한글 표나 엑셀에서 그대로 복사해 붙여도 된다(칸이 탭으로 나뉜다).

```
문제                          정답        오답 보기들
지구에서 가장 큰 바다는?       태평양      대서양   인도양   북극해
7 × 8 은?                     56          54      63      48
고래는 물고기이다.             X
물이 얼면 무엇이 되나요?       얼음
세종대왕이 만든 글자는?        한글/훈민정음
```

- **둘째 칸이 정답.** 그 뒤는 전부 오답 보기다. 보기 순서는 알아서 섞인다.
- 오답 보기를 안 적으면 **답을 직접 쓰는 문제**가 된다.
- 정답을 `O` 나 `X` 로 적으면 **OX 문제**가 된다.
- 정답이 여러 개 맞을 땐 `한글/훈민정음` 처럼 **/** 로 나눈다.
- 답을 견줄 때 **띄어쓰기·물음표·쉼표는 무시**하고, 영어 대소문자와 전각 숫자도 맞춰 준다.
  (`태평양` = ` 태평양 ` = `태 평 양`)

---

## 6. 아바타와 골드

아이템은 **1498가지**다. 그림 파일은 한 장도 안 쓴다 — 전부 코드로 그린다.
그래서 크롬북에서 가볍고, 색을 바꾸면 같은 옷이 색깔별로 다 나온다.

큐플레이 느낌이 나도록 이렇게 만들었다.

- **얼굴** — 눈썹(머리 색을 따라감), 두꺼운 윗속눈썹과 바깥쪽 삐침,
  홍채 네 겹 + 반짝이 세 개, 갸름한 턱
- **머리** — 끝을 뾰족하게, 머릿결 선, 모양마다 제자리에 붙는 윤기
  (윤기를 한 자리에 고정했더니 검은 머리에서 땜통처럼 보여서 모양별로 옮겼다)
- **몸** — 팔을 몸통에서 떼어 겨드랑이에 틈이 보이게. 붙어 있으면 한 덩어리처럼 뭉툭하다
- **옷** — 주름선·소매단·단추·깃·주머니. 소매는 공용 부품이라
  팔 위치를 한 번 바꾸면 옷 14벌이 같이 따라온다
- **선(라인아트)** — `ink()` 함수 한 개가 `paint-order` 로 처리해서
  1112종 전부에 한꺼번에 들어간다. 아이템을 새로 넣어도 선은 저절로 붙는다
- **배경 장식** — 캐릭터 뒤에 깔리는 그림. 홈·상점·패션왕 화면처럼
  크게 보는 곳에서만 나오고, 명단 같은 작은 그림에는 안 나온다 (시끄러워서)

| 칸 | 모양 수 | 색 |
|---|---|---|
| 머리 | 18 (남자 머리 6종 포함) | 12색 |
| 상의 | 14 | 10색 |
| 하의 | 11 | 10색 |
| 신발 | 9 | 10색 |
| 모자·머리장식 | 20 (카우보이·해적·군인·경찰·요리사·헤드폰…) | 10색 |
| 안경 | 12 | 10색 |
| **수염·점** (콧수염·턱수염·주근깨·반창고·마스크…) | 12 | 12색 |
| 귀걸이 | 9 | 6색 |
| 목걸이 | 9 | 6색 |
| 등 장식 (날개·망토·가방·후광…) | 9 | 10색 |
| 펫 (강아지·고양이·펭귄·공룡·판다·여우·유니콘·꿀벌…) | 16 | 10색 |
| **배경 장식** (정원·밤하늘·바다·우주·무대…) | 12 | 10색 |

**남자 / 여자**도 공짜로 아무 때나 바꾼다. (옷 가게 → 얼굴·눈 바꾸기)
남녀는 몸이 아니라 **얼굴과 어깨**에서 갈린다 — 눈 크기, 눈썹 굵기, 속눈썹,
입 너비, 어깨 넓이. 몸 자체를 바꾸면 옷 14벌이 다 안 맞기 때문에
옷이 든 상자를 통째로 가로로 늘이는 방식을 썼다.

피부색·눈 색·눈 모양도 **공짜**다.

### 캐릭터가 살아 있다

- **눈을 깜빡인다.** 아바타마다 박자가 다르다 (다 같이 깜빡이면 기계 같다)
- **숨쉬듯 살짝 움직인다**
- **펫이 깡충거리고** 꼬리·귀·날개를 흔든다
- **배경도 움직인다** — 구름이 흐르고, 눈과 꽃잎이 내리고, 별이 반짝이고,
  물결이 일고, 색종이가 떨어지고, 행성이 돈다

움직임은 **크게 보이는 화면에서만** 켜진다. 명단·OX처럼 21명이 한꺼번에
뜨는 곳과 패션왕 심사 화면에서는 전부 멈춘다. 크롬북이 버거워지지 않게 한 것이다.
전부 `transform` 만 써서 브라우저가 GPU 로 처리한다.

### 골드 규칙 — 파일 맨 위 `window.RULES` 에서 고친다

| 항목 | 지금 값 | 뜻 |
|---|---|---|
| `startGold` | 300 | 가입할 때 주는 골드 |
| `practicePerQ` | 10 | 혼자 연습에서 한 문제 맞힐 때 |
| `practiceCap` | 300 | **혼자 연습으로 하루에 벌 수 있는 최대** |
| `practiceCount` | 10 | 연습 한 판 문제 수 |
| `livePerQ` | 25 | 수업 모드에서 한 문제 맞힐 때 |
| `liveRankBonus` | [120, 80, 50] | 수업 끝나고 1·2·3등 보너스 |
| `answerSec` | 20 | 한 문제 제한 시간(초) — 방 열 때도 바꿀 수 있다 |
| `fashionPrize` | [500,300,150] | 패션왕 1·2·3등 상금 |
| `fashionJoin` | 50 | 패션왕에 참가만 해도 주는 골드 |

**하루 상한이 있는 이유:** 연습 모드는 혼자 무한정 풀 수 있어서, 상한이 없으면
수업에 안 오고 연습만 돌려도 다 살 수 있게 된다. 수업 모드에는 상한이 없다.

값이 크면 금방 다 사서 시들해지고, 작으면 지쳐서 포기한다.
지금 값이면 **싼 옷은 몇 판 만에, 펫이나 날개 같은 건 며칠 모아야** 산다.
한 주 해 보고 아이들 반응을 보고 고치는 게 낫다.

선생님 화면 **학생 명단** 에서 개인·전체에게 골드를 더 줄 수도 있다.

---

### 6-2. 패션왕 대회 — 매주 금요일

**이게 이 게임의 심장이다.** 퀴즈로 번 골드를 옷에 쓸 이유를 만들어 준다.
잘 꾸민 아이가 상을 받아야 "골드를 왜 모으나"가 생긴다.

**선생님이 하는 일 (5분이면 끝난다)**

선생님 화면 → **👑 패션왕** 탭

1. **대회 열기** — 주제를 적는다 (예: "겨울옷", "우리 반 캐릭터", 그냥 확인하면 "자유 꾸미기")
2. 아이들이 참가한다. **지금 입고 있는 옷 그대로** 들어간다.
   더 꾸미고 와서 다시 참가해도 된다 (덮어쓴다)
3. **투표 시작** — 서로 한 표씩. **자기 자신에게는 못 한다.** 한 번 하면 못 바꾼다
4. **결과 발표 + 상금 주기** — 누르면 상금이 그 자리에서 각자 계정에 들어간다

**상금**: 1등 500 / 2등 300 / 3등 150골드, **참가만 해도 50골드**.
1등은 이름 옆에 **왕관 👑** 이 붙고 (여러 번 타면 여러 개),
**역대 패션왕** 명단에 이름이 남는다.

**금요일이 되면** 아이들 홈 화면에 저절로 안내가 뜬다 —
"오늘은 금요일 — 패션왕 대회 날!". 선생님 화면에도 같은 알림이 뜬다.

> 참가만 해도 50골드를 주는 이유: 안 꾸민 아이가 창피해서 아예 안 들어오는 걸 막으려는 것이다.
> 들어오면 뭐라도 받아 가야 다음 주에 또 온다.

**주제를 바꿔 가며 하면 오래 간다.** 예) 겨울옷 / 파란색만 / 제일 웃긴 옷 /
안경 쓴 사람 / 펫이랑 어울리게. 옷값이 안 드는 주제(예: "제일 웃긴")를 섞으면
골드가 적은 아이도 이길 수 있다.

### 6-3. 화면에 대해

보내 주신 **큐플레이 참고 사진 다섯 장**을 보고 화면마다 배경을 따로 그렸다.
그림 파일은 하나도 안 쓴다 — 전부 코드로 그린 것이라 받아올 게 없다.

| 화면 | 어떻게 생겼나 |
|---|---|
| **시작(로그인)** | 하늘색 하늘에 큰 물방울이 떠 있고 그 위에 남·여 캐릭터가 서 있다. 가운데 `L·O·G·I·N` 남색 판에 아이디 칸·비밀번호 칸·노란 `GO` 단추 |
| **수업 — 일반 퀴즈** | 방송국 스튜디오. 위에 조명 트러스, 양옆에 조명 기둥, 가운데 큰 TV 모니터에 문제와 보기, 아래 무대에 **반 친구들이 책상 뒤에 줄지어** 서 있다 (답을 낸 친구는 책상이 초록색) |
| **수업 — OX** | 잔디 들판. 위쪽 나무 현판에 문제, 그 아래 `O ○명 vs ○명 X` 검은 점수판, 흙마당 왼쪽에 분홍 O 고리 · 오른쪽에 파란 X. 고른 쪽으로 캐릭터가 뛰어간다 |
| **수업 — 가로세로** | 하얀 돌성. 좌우 탑 창문에 잘하는 친구 여섯이 서고 이름 깃발이 걸린다. 가운데 성문 안에 낱말판, 오른쪽에 양피지 문제판 |
| **끝나고 (결과)** | 별과 행성이 뜬 밤하늘. 큰 `WINNER` 글자 뒤로 비치는 순위표(등수·이름·점수·맞힌 문제·보너스), 왼쪽 단상 위에 1등 캐릭터 |

21명이 다 들어와도 스크롤 없이 한 화면에 들어가게 맞춰 뒀다.

**아래 알림 띠**에 도는 문구는 `index.html` 안에서 `const TIPS = [` 를 찾으면 나온다.
반에 맞게 고쳐 쓰면 된다. 예) "이번 주 패션왕 주제는 겨울옷!"

> 참고 사진은 **배치와 분위기를 보는 용도로만** 썼다.
> 큐플레이의 실제 그림 파일을 게임에 넣어 배포하면 저작권 문제가 되기 때문에,
> 나오는 그림은 전부 새로 그린 것이다.

### 6-4. 소리

**파일을 하나도 안 쓴다.** 브라우저가 직접 소리를 만든다(웹오디오).
그래서 받아올 게 없고 크롬북에서 로딩이 안 늘어난다.

- **효과음** — 버튼 딸깍, 답 고르기, 정답, 오답, 골드, 물건 사기,
  마지막 5초 초읽기, 팡파레
- **배경음악** — 통통 튀는 8마디가 계속 돈다

**소리 끄기**는 화면 위 🔊 단추다. 껐다 켠 것은 그 크롬북에 기억된다.
브라우저 규칙상 **아이가 화면을 한 번 눌러야** 소리가 나기 시작한다.
(가만히 두면 소리가 안 나는 게 정상이다)

수업에 방해되면 처음에 다 같이 끄고 시작해도 된다.

### 6-5. 가로세로 낱말은 저절로 만들어진다

가로세로는 **선생님이 문제를 안 만들어도 된다.**
수업 열기 → 가로세로 → **낱말 주제**만 고르면 격자가 저절로 짜인다.

주제: 동물 · 식물 · 우리 몸 · 날씨와 계절 · 우리 고장 · 수학 · 국어 · 학교생활 · 음식

3학년 교과에 맞으면서 **서로 글자가 잘 겹치는 낱말**로 골라 담아 뒀다.
아무 낱말이나 넣으면 겹칠 데가 없어 격자가 밋밋해지기 때문이다.
직접 만든 문제 묶음으로 하고 싶으면 아래에서 묶음을 고르면 된다.

**아이들은 격자 칸을 눌러서** 낱말을 고른다. 두 낱말이 지나는 칸을 다시 누르면
가로/세로가 번갈아 골라진다. 고른 낱말의 힌트는 아래 답안지에 흐리게 나온다.

## 7. 알아 둘 것 (솔직하게)

**① 문제 은행은 마음먹으면 볼 수 있다.**
아이들이 혼자 연습하려면 문제를 읽어 와야 해서, 문제와 정답이 데이터베이스에 있고
개발자 도구를 열 줄 알면 볼 수 있다. 초등 3학년한테는 사실상 문제가 안 되지만,
**중요한 시험을 이걸로 보지는 마라.**

다만 **수업 모드에서는 정답을 공개 전까지 방에 안 올린다.** 채점도 선생님 화면이 한다.
그래서 통신을 들여다봐도 다음 문제 답은 안 보이고, 아이가 자기 점수를 올릴 수도 없다.

**② 비밀번호는 그대로 저장하지 않지만, 진짜 암호는 아니다.**
숫자 4자리를 섞어서 저장한다. 옆자리 아이가 값만 보고 알아채지는 못한다.
하지만 숫자 4자리는 경우의 수가 1만 개뿐이라 **작정하면 뚫린다.**
교실용으로 충분한 수준이지, 진짜 계정 보안이 아니다.

**③ 데이터베이스 규칙이 열려 있다.**
로그인 없이 쓰는 구조라 주소를 아는 사람은 데이터를 고칠 수 있다.
양들의 밤도 지금 같은 상태다. 반 게임에는 보통 문제가 안 되지만,
걱정되면 파이어베이스 **익명 로그인**을 켜고 규칙을 `"auth != null"` 로 바꾸는 방법이 있다.
(코드도 조금 손봐야 한다. 필요하면 그때 하자)

**④ 인터넷이 있어야 한다.** 파이어베이스를 인터넷에서 받아온다.

---

## 8. 크롬북 성능

3D가 아니라 그림(SVG)과 글자만 그린다. 양들의 밤과는 짐이 다르다.
개발 PC에서 잰 값 — **21명 아바타를 한 번에 그려서 화면에 붙이는 데 14ms**,
한 번 그린 아바타는 저장해 뒀다가 다시 쓰므로 두 번째부터는 0ms.
크롬북이 20배 느려도 200ms 정도라 한 화면 넘어갈 때 한 번 드는 값이다. 넉넉하다.

그림 자세함을 세 단계로 나눠 뒀다.

| 단계 | 어디에 | 무엇이 나오나 |
|---|---|---|
| 3 | 홈·옷 가게·연습·수업 왼쪽 카드 (한 명만 크게) | 전부 + **움직임** |
| 2 | 패션왕 심사 (21명이 한꺼번에) | 배경·등장식·펫은 나오되 **정지** |
| 1 | 명단·OX·자리 (작게 여러 명) | 배경·등장식·펫 **안 그림**, 깜빡임만 |

제일 무거운 장면인 OX 무대 21명이 15ms 인 이유가 이것이다.

배경 무늬와 떠다니는 장식은 **한 번만 칠해지고 안 움직인다.** 그래서 공짜다.
큐플레이 느낌을 내려고 그림자에 흐림(blur)을 쓰지 않고 전부 단색 그림자만 썼다 —
양들의 밤 때 배운 대로, 흐림은 이 크롬북의 GPU 에서 비싸다.

그래도 **판단은 교실 크롬북에서 해라.** (양들의 밤에서 겪은 그대로다)
느려지면 제일 먼저 볼 곳은 **아이들이 많이 모인 화면**
— 수업 모드 아래쪽 자리 줄과 OX 무대다. 아바타 크기를 줄이면 바로 가벼워진다.

---

## 9. 파일이 어떻게 짜여 있나

`퀴즈게임/index.html` 한 파일 안에 순서대로 들어 있다.

| 번호 | 무엇 |
|---|---|
| 1 | 자잘한 도구 (답 견주기, 비번 섞기) |
| 2 | **아바타** — 몸·얼굴·눈, 머리·옷·신발·모자·안경·귀걸이·목걸이·등장식·펫 모양 함수 |
| 3~4 | 파이어베이스, 내 계정 |
| 5~8 | 화면 바꾸기, 로그인, 홈, 옷 가게 |
| 9~10 | 문제 은행, 선생님 화면 |
| 11~12 | 문제 그리는 공용 부품, 혼자 연습 |
| 13~14 | 수업 방 규칙 + 가로세로 격자 짜기, 선생님 조종판 |
| 15 | 학생 수업 화면 (일반 / OX / 가로세로) |
| 16 | 패션왕 대회 (학생 화면 + 선생님 조종판) |

**아이템을 더 넣고 싶으면** 2번 구역의 `TOPS`, `HATS`, `PETS` 같은 목록에
`이름:['보이는 이름', 가격, (c,d,l)=> 그림]` 을 한 줄 더 쓰면 된다.
`c` 는 고른 색, `d` 는 그보다 어두운 색, `l` 은 밝은 색이 저절로 들어온다.
색 개수만큼 아이템이 저절로 늘어나고, 선(라인아트)도 저절로 붙는다.

**상의**는 공용 부품을 쓴다. `PA(T_BASE,c)` 로 몸통을 깔고 `slv('l',c)` 로 소매를 단다
(`s`짧은 / `l`긴 / `p`퍼프 / `w`넓은 / `n`없음). 그 위에 깃·단추·주름만 얹으면 된다.
**배경**은 `BGS` 에 200×300 짜리 그림을 그리면 된다. 모서리 둥글리기는 CSS 가 한다.
